import React from "react";

function Breadcrumb({ pageTitle }) {
  return (
    <section className="visual lawyer">
      <div className="container">
        <h1 className="ctg-head">{pageTitle}</h1>
      </div>
    </section>
  );
}

export default Breadcrumb;
